import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'aws-amplify-cp-ionic7-react',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
